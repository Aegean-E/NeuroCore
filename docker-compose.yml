version: '3.8'

services:
  neurocore:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./settings.json:/app/settings.json
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
